import './css/ColorPicker.css';

const ColorPicker = ({colorSetter}) => {
    return (
        <div className="ColorPicker">
            <canvas></canvas>
        </div>
    );
};

export default ColorPicker;